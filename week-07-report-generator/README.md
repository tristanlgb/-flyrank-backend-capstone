# Week 7 — PDF Report Generator

Planned reporting pipeline using SQLite aggregation, HTML-to-PDF rendering,
file storage, API download links, and duplicate-request protection.

The implementation will use real project data and produce a clean report of at
least two pages without embedding PDF bytes inside JSON responses.
